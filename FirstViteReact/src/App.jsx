import Method from "./Fun,";
function App() {
  const username="AYUSHHHHHHH"
  return (  // in react project we can only add one component inside return function "<></>" it is called fragment everything inside this is treated as single component
  <>
  <Method />
  <h1>user is {username}</h1>
  <h1>HELLO AYUSH,FIRST REACT PROJECT WITH VITE !!!</h1>
  </>
  );
}

export default App
